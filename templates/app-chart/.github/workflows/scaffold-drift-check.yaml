name: Scaffold Drift Check (Manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  drift-check:
    uses: orhayoun-eevee/build-workflow/.github/workflows/scaffold-drift-check.yaml@__BUILD_WORKFLOW_REF__
    with:
      repo_name: __REPO_NAME__
    secrets:
      gh_app_id: ${{ secrets.GHCR_AUTO_APP_ID }}
      gh_app_private_key: ${{ secrets.GHCR_AUTO_PKEY }}
