name: Dependency Review (Manual / Break Glass)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: dependency-review-manual-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dependency-review:
    uses: orhayoun-eevee/build-workflow/.github/workflows/dependency-review.yaml@__BUILD_WORKFLOW_REF__
